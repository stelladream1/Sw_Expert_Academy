T = int(input())
for test_case in range(1, T+1):
    n, a, b = map(int, input().split(" "))
    max_value = min(a, b)

    if a + b - n <= 0:
        min_value = 0
    else:
        min_value = a + b - n

    print(f"#{test_case}", max_value, min_value)

