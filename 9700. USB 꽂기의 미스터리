t = int(input())
for test_case in range(1,t+1):
    p,q = map(float,input().split())
    num1 = (1-p) *  p*q
    num2 = p * (1-q) * p * q
    if num1 < num2:
        print(f"#{test_case} YES")
    else:
        print(f"#{test_case} NO")
